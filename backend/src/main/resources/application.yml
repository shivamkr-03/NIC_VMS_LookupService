server:
  port: 8082

spring:
  application:
    name: lookup-service

  datasource:
    url: jdbc:mysql://localhost:3306/sugam
    username: root
    password: root123

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true